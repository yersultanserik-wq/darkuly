package com.dyingdark.items;

public enum WeaponShape {
    CROSS,   // sword: + shape
    AXE,     // wide top triangle + handle
    BOW,     // arc + string
    STAFF,   // tall vertical + orb
    DAGGER   // thin narrow blade
}
